<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class='navbar-brand' href="/" >READ IT LATER</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if account_signed_in? %>
            <% if current_account.has_attribute?("level") && current_account.level == 1 %>
                <li>
                  <input type="button" class="btn btn-default navbar-btn" value="VIEW REPORT" onclick="window.location.href='<%= reports_path %>'" />
                </li>
            <% end %>
            <li>
               <input type="button" class="btn btn-default navbar-btn" value="New Link" onclick="window.location.href='<%= new_link_path %>'" />
            </li>
            <li><%= link_to('LOGOUT', destroy_account_session_path, :method => :delete) %></li>
        <% else %>
          <li><%= link_to('SIGN UP', new_account_registration_path) %></li>
          <li><%= link_to('LOGIN', new_account_session_path) %></li>
        <% end %>
      </ul>
    </div>
    <div class="clearfix"></div>
  </div>
</nav>
